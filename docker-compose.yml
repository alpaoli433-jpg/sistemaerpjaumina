
services:
  postgres:
    image: postgres:16-alpine
    container_name: cocteleria_db
    restart: unless-stopped
    ports:
      - '5432:5432'
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: cocteleria_db
    volumes:
      - cocteleria_pgdata:/var/lib/postgresql/data

volumes:
  cocteleria_pgdata:
